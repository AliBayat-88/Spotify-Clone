function EmptyIcon({size}) {
  return (
    <span className={`${size} rounded-full border border-gray-500 flex items-center justify-center text-[10px] font-bold shrink-0`}>
              ✓
            </span>
  );
}

export default EmptyIcon;
